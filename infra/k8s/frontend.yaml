---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: dvf
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: acrdvflucaszub.azurecr.io/frontend:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "250m"

---
# Service LoadBalancer : expose le frontend sur une IP publique Azure
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: dvf
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
  type: LoadBalancer
